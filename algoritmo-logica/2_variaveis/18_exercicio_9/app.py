"""
crie algumas variaveis com o valor de velocidade
crie tambem uma variavel limite com valor de 80
imprima expressoes logicas que verificam se a velocidade são maiores ou iguais a 80
"""
velocidade_a = 90
velocidade_b = 80
velocidade_c = 60

limite = 80

print(velocidade_a >= limite)
print(velocidade_b >= limite)
print(velocidade_c >= limite)